spring:
  application:
    name: spring-cloud-config-client-feign

server:
  port: 8767

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/


feign:
  hystrix:
    enabled: true #开启熔断器